task :my_task do
  puts 'Task is running'
end

# pose: Rake is a popular task manager and build automation tool that comes with Ruby.
# Rake is often used for managing various tasks related to the application,
# such as running database migrations, seeding the database, running tests, and more.
# Example: You might run rake db:migrate to apply pending database migrations.

# rake -T
# Rake task_name
# rake db:migrate
# Rake is a powerful tool, it's commonly used for tasks like database management, running tests, 
# and other routine activities in a Rails project.